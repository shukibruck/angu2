import { Component, EventEmitter, ViewEncapsulation } from '@angular/core';

@Component({
    selector: 'my-app',
    templateUrl: '/Scripts/app/manview.html',
    encapsulation: ViewEncapsulation.None
})

export class AppComponent {
    
}